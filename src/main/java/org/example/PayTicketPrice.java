package org.example;

import java.util.UUID;

public class PayTicketPrice {

    public String payFinalPrice(double totalFinalPrice) {
        UUID uuid = UUID.randomUUID();
        return uuid.toString();
    }
}
